import { Container } from "@/components/layout/container";

export default function AboutPage() {
  return (
    <Container className="py-24 md:py-32">
      <h1 className="m-0 text-5xl font-medium tracking-[-0.04em] md:text-7xl">About</h1>
      <p className="mt-6 max-w-xl text-[var(--muted)]">Foundation route placeholder. Final approved page composition will be implemented in the next build slice.</p>
    </Container>
  );
}
